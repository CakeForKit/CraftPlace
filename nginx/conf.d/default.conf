# events {
#     worker_connections 16;
# }

# http {
#     # include /etc/nginx/mime.types;
#     default_type application/octet-stream;

#     upstream app_craftplace {
#         server app_craftplace:8080;
#     }

    server {
        listen 80;
        server_name localhost;

        # # Статические файлы
        # location /static/ {
        #     alias /usr/share/nginx/static/;
        #     expires 1y;
        #     add_header Cache-Control "public, immutable";
        # }

        # Главная страница
        location / {
            root /usr/share/nginx/static;
            index index.html;
            try_files $uri $uri/ /index.html;
        }

        # # API routes - проксируем к Go-приложению
        # location /api/ {
        #     proxy_pass http://app_craftplace;
        #     proxy_set_header Host $host;
        #     proxy_set_header X-Real-IP $remote_addr;
        #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        #     proxy_set_header X-Forwarded-Proto $scheme;
            
        #     # Swagger UI
        #     location /api/v1 {
        #         proxy_pass http://app_craftplace/api/v1;
        #     }
        #     location /api/v2 {
        #         proxy_pass http://app_craftplace/api/v2;
        #     }
        # }

        # # Legacy interface
        # location /legacy {
        #     # Если есть legacy приложение - проксируем
        #     # Если нет - отдаем статическую страницу
        #     root /usr/share/nginx/html;
        #     try_files /legacy.html /index.html;
        # }

        # # Documentation - README.md
        # location /documentation {
        #     # Можно отдать raw markdown или сгенерировать HTML
        #     alias /usr/share/nginx/html/README.md;
        #     default_type text/markdown;
        # }


        # # Status page
        # location /status {
        #     stub_status on;
        #     access_log off;
        #     allow all; # В продакшене ограничьте доступ!
        # }

        # # Reserved path - дублирует главную
        # location /reserved/ {
        #     rewrite ^/reserved/(.*)$ /$1 break;
        #     root /usr/share/nginx/html;
        #     try_files $uri $uri/ /index.html;
        # }

        # # Management page
        # location /management {
        #     root /usr/share/nginx/html;
        #     try_files /management.html /index.html;
        # }
    }
# }